# Torres Vigilância Patrimonial - Website & Sistema Interno

## ⚠️ SISTEMA DE CONTEXTO MESTRE
**Antes de qualquer tarefa, leia o arquivo `SYSTEM_BRAIN.md` na raiz do projeto.** Ele contém regras primordiais de timezone, finanças, arquitetura, dependências e lições aprendidas que NUNCA devem ser violadas.

## 🏦 Integração Banco Inter (Maio/2026)

Banco Inter foi integrado para **Cobrança (boleto+PIX) + Banking (extrato/saldo/pagamentos)**, substituindo o Asaas no fluxo de cobrança. Asaas permanece exclusivamente para emissão de NFS-e.

### Arquitetura
- **`server/services/inter/client.ts`** — cliente HTTP nativo via `https.request` com mTLS (cert .crt + .key). OAuth2 client_credentials com cache de token automático. Singleton `getInterClient()`.
- **`server/services/inter/cobranca.ts`** — CRUD de cobranças (criar, consultar, cancelar, PDF, listar) + cadastro de webhook.
- **`server/services/inter/banking.ts`** — saldo, extrato, extrato/completo, pagamento de boleto, PIX por chave/dados bancários.
- **`server/routes/inter.ts`** — rotas Express completas em `/api/inter/*`. Webhook público em `/api/inter/webhook/cobranca` sempre retorna 200 (Inter retenta infinito em 4xx/5xx).
- **`server/routes.ts:39 ensureInterTables()`** — cria via Supabase RPC as 3 tabelas Inter (`inter_extrato_lancamentos`, `inter_pagamentos`, `inter_webhook_events`) + colunas `gateway` e `inter_codigo_solicitacao` em `invoices`. Roda na startup, idempotente.
- **`server/cron.ts`** — job a cada 30min lê extrato Inter dos últimos 7 dias, grava em `inter_extrato_lancamentos`, casa entradas (créditos) com `invoices` por valor + status PENDING/OVERDUE e marca como RECEIVED. Pula silenciosamente se Inter não estiver configurado (`isInterConfigured()`).
- **Frontend:** `client/src/pages/admin/inter-extrato.tsx` (extrato com filtros + CSV) e `contas-a-pagar.tsx` (despesas pendentes com pagamento via PIX/Boleto Inter). Toggle "Gateway: Asaas | Inter" em `faturas.tsx` (CreateInvoiceDialog) com persistência em localStorage.

### Variáveis de ambiente necessárias
- `INTER_CLIENT_ID`, `INTER_CLIENT_SECRET` — credenciais OAuth2 do app PJ no IB
- `INTER_CONTA_CORRENTE` — conta corrente sem hífen
- `INTER_CERT_CRT`, `INTER_CERT_KEY` — conteúdo PEM do certificado e chave (gerar via openssl + CSR no IB PJ)
- `INTER_AMBIENTE=prod` ou `sandbox` (default: prod). Sandbox: `https://cdpj-sandbox.partners.uatinter.co`. Prod: `https://cdpj.partners.bancointer.com.br`.

### Escopos OAuth2 obrigatórios no IB PJ
- `boleto-cobranca.read`, `boleto-cobranca.write` (cobrança)
- `extrato.read` (extrato)
- `pagamento-boleto.read`, `pagamento-boleto.write` (pagamento de boletos)
- `pagamento-pix.write` (PIX)
- `webhook-banking.write`, `webhook-cobranca.write` (webhooks)

## Overview
This project delivers an institutional landing page and an internal management system for Torres Vigilância Patrimonial, a Brazilian security company. The system's core purpose is to streamline operational workflows, manage company assets, track personnel and vehicles, and enhance overall operational efficiency. It provides robust user and role management, detailed asset tracking (vehicles, weapons), automated operational processes (service orders, mission workflows), and real-time operational oversight. The strategic goal is to provide a comprehensive, integrated platform that supports company growth, ensures regulatory compliance, and elevates the quality of security services.

## User Preferences
I prefer clear and direct communication. When making changes, prioritize iterative development with clear explanations for each step. For any significant architectural or design decisions, please ask for my approval before proceeding. Ensure all code is well-documented and follows modern TypeScript and React best practices. I value a clean, maintainable codebase.

## ⛔ REGRAS CRÍTICAS — LEIA PRIMEIRO

### Diretrizes Obrigatórias de Desenvolvimento

1. **Idioma e Comunicação:** Código documentado em português. Variáveis e comentários em português quando possível.
2. **Verificação de Banco (Supabase):** Antes de qualquer alteração de lógica, verificar se as tabelas (financial_transactions, service_orders, mission_costs, etc.) existem e se os campos são persistentes. É proibido usar memória temporária ou localStorage para dados financeiros.
3. **Sincronismo Global (Realtime):** Qualquer atualização (Pedágio, Abastecimento, Status) deve usar `supabase.channel` para atualizar todas as abas abertas simultaneamente sem necessidade de F5.
4. **Prevenção de Erros Futuros:** O código deve ser escalável. A lógica de data (created_at) NÃO pode esconder custos de ontem no DRE de hoje. Custos de abastecimento devem ser persistentes até o fim da missão.
5. **Memória de Regras:** Ler este arquivo antes de cada commit. Se o código violar uma regra de negócio (como o pedágio ser custo e receita ao mesmo tempo), abortar e corrigir.
6. **Saúde do Sistema:** Não implementar soluções hardcoded (valores fixos no código). Tudo deve vir de consultas dinâmicas ao banco.
7. **Verificação Grid ↔ DRE:** Antes de finalizar qualquer alteração financeira, verificar se o dado reflete tanto no Grid Operacional quanto no DRE ao mesmo tempo.

### Regras de Dados e Acesso

- **⛔ PROIBIDO USAR PostgreSQL DIRETO (Drizzle/db.*).** É TERMINANTEMENTE PROIBIDO usar `db.*` (Drizzle ORM), `db.execute()`, `db.select()`, `db.insert()`, `db.update()`, `db.delete()` ou qualquer acesso direto ao PostgreSQL para operações CRUD. TODO acesso a dados DEVE usar exclusivamente a Supabase REST API (`supabaseAdmin.from(...)`). A ÚNICA exceção é `db-init.ts` para migrações DDL (ALTER TABLE, CREATE TABLE, CREATE INDEX) que a REST API não suporta. A OUTRA exceção é `server/pg-fallback.ts` que usa PostgreSQL local (DATABASE_URL) como cache/fallback para leitura quando o Supabase REST API está indisponível.
- **Data Access Paths (REGRA ATUAL):**
    1. `storage.*` (em `server/storage.ts`) — usa Supabase REST API (`supabaseAdmin`) com conversão automática snake_case↔camelCase. **Inclui fallback resiliente para PostgreSQL local** quando Supabase falha.
    2. `supabaseAdmin.from(...)` — chamadas REST API diretas em CRON, routes e operações específicas. Retorna snake_case. Helpers `resilientSupabaseSelect`/`resilientSupabaseSingle` em `_helpers.ts` fornecem fallback.
    3. ❌ `db.*` (Drizzle ORM) — **PROIBIDO para CRUD**. Só permitido em `db-init.ts` para DDL de schema.
- **Resiliência de Banco (pg-fallback) — DESATIVADO POR PADRÃO:** Por decisão do produto, o sistema opera **100% no Supabase**. O fallback PostgreSQL local foi desativado via flag `DISABLE_LOCAL_FALLBACK=true` (default). Quando ativo (set `DISABLE_LOCAL_FALLBACK=false` para habilitar), os componentes abaixo entram em ação:
    1. `server/supabase.ts` — **Semáforo de concorrência** (máx 6 requests simultâneos ao Supabase). Retry automático (3 tentativas, timeout 15s, jitter aleatório). **Janela deslizante** de health (20 amostras): 60% falha = offline, 30% falha = recuperado. Cooldown 90s anti-flapping. Trata HTTP 521/502/503 como retry-able.
    2. `server/pg-fallback.ts` — Pool singleton ao PostgreSQL local (DATABASE_URL do Replit), sync periódico com stagger 800ms entre tabelas (não dispara em rajada), early abort após 3 falhas consecutivas, fallback de leitura. `cacheRows` usa `DISABLE TRIGGER ALL` + SAVEPOINTs para evitar erros de FK constraint.
    3. `server/storage.ts` — `resilientList`/`resilientGet`/`resilientInsert`/`resilientUpdate`/`resilientDelete` consultam o PostgreSQL local quando o Supabase falha (apenas se a flag estiver desativada). Com a flag ATIVA (default), esses helpers fazem **throw** do erro do Supabase em vez de cair em fallback silencioso.
    4. `GET /api/health` — Retorna `supabase: "online"/"offline"`, `localDb: "online"/"offline"`, `mode: "primary"/"fallback"`.
    5. **Alertas por E-mail:** `pg-fallback.ts` envia alerta automático para `thiago@grupotmseg.com.br` (Supabase down/up). Cooldown 10min.
    6. **Write Queue (fallback_write_queue):** Quando Supabase falha em INSERT/UPDATE/DELETE e a flag de fallback está habilitada, a escrita é salva na tabela local `fallback_write_queue`. Um worker a cada 30s (`flushWriteQueue`) tenta reprocessar os itens pendentes. Máximo 10 tentativas por item. Endpoints: `GET /api/write-queue/stats`, `POST /api/write-queue/flush`. Com a flag ATIVA (default), nem `enqueueWrite` nem `flushWriteQueue` rodam — todas as falhas de gravação propagam ao cliente como HTTP 500.
    7. **Bug histórico corrigido (28/04/2026):** O `resilientUpdate` engolia falhas: quando o `applyViaDirectSql` (via `SUPABASE_DATABASE_URL`) também falhava (por exemplo, coluna inexistente), o erro era logado e a escrita caía silenciosamente em `enqueueWrite`, fazendo o backend responder 200 com o payload sem nada ter sido salvo. Hoje, quando o SQL direto falha, `resilientInsert/Update` re-lançam o erro. Sintoma original: campos de e-mail (contratual/operacional/medição/financeiro) e `razaoSocial`/`nomeFantasia` em `/admin/clients` não persistiam. As colunas `razao_social` e `nome_fantasia` foram adicionadas à tabela `clients` no Supabase e em `db-init.ts` (idempotente).
- **Conexões de Banco:**
    - `DATABASE_URL` (runtime-managed pelo Replit) — PostgreSQL local para fallback/cache.
    - `SUPABASE_DATABASE_URL` (env var compartilhada) — Conexão direta ao Supabase via pgbouncer na porta 6543 (`?pgbouncer=true`). Usado pelo `db-init.ts` (DDL) e `drizzle.config.ts` (migrações).
    - `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` — REST API do Supabase (principal via de acesso a dados).
    - `db-init.ts` usa PG client singleton (não cria novo Client a cada DDL), fecha automaticamente após init.
- **NEVER add a `password` column** back to `shared/schema.ts` — authentication is handled entirely by Supabase Auth.
- **Always use `apiRequest()` or `authFetch()`** for API calls — never raw `fetch()`.
- **OS status values are stored with accents** (e.g., `"concluída"`) — always normalize before comparing.
- **`data_missao` must be stored as a full ISO timestamp** (e.g., `"2026-04-02T11:00:00"`) — never a date-only string (`"2026-04-02"`). PostgreSQL stores date-only strings as UTC midnight, which shifts the BRT date by one day back.
- **100% PADRÃO BRASIL / HORÁRIO DE BRASÍLIA (BRT, UTC-3):** REGRA ABSOLUTA — todo o sistema opera exclusivamente em Horário de Brasília. Toda data, hora, gravação, cálculo, exibição e comparação DEVE usar BRT. O banco armazena timestamps sem timezone **em BRT nativo** (não UTC). O OID parser 1114 em `db.ts` adiciona `-03:00` ao ler. `SET timezone = 'America/Sao_Paulo'` no pool DB. `localInputToUtc()` DESATIVADA — repassa BRT direto. `nowBRTString()` gera timestamps BRT. No frontend, `parseUTCDate()` adiciona `-03:00`. NUNCA usar `.toISOString()` para gravar (converte para UTC = shift +3h). Não existe nenhum componente que opere em UTC puro.
- **`missionStartedAt` é setado no primeiro checkout** (checkout_armamento), não no em_transito_origem. Isso garante que o cronômetro reflita o tempo real da jornada completa.
- **Billing hora extra usa `missionStartedAt`** como início de cobrança. O cálculo de horas usa a RPC `calc_mission_elapsed_hours()` do banco (NOW() do Supabase) como fonte única de tempo. A função centralizada `calcularFaturamentoLive()` em `server/billing-calc.ts` é chamada pelo Grid, DRE, CRON e Dashboard. Hora Extra é RECEITA cobrada do cliente, nunca despesa de funcionário.
- **Sincronismo Total de Indicadores:** Todos os indicadores gerenciais (Dashboard, Balanço, Metas, Grid) devem ser re-calculados dinamicamente com base nos dados brutos das missões, garantindo 100% de paridade com os DREs individuais. É proibido ter valores diferentes em telas diferentes.
- **CRON billing** MUST use `supabaseAdmin.from("service_orders")` (REST API with snake_case columns) — NEVER `storage.getServiceOrders()`. Only recalculates active missions; concluded missions are never touched.

### Regras de Persistência Financeira

- **Custos operacionais devem ser calculados dinamicamente no banco** (`mission_costs` table) e refletidos via Realtime no Grid e DRE. É proibido usar campos totalizadores estáticos ou cache de servidor que gerem dessincronia.
- **Custo de abastecimento = mission_cost vinculado à OS.** Toda vez que um fueling é registrado (mobile ou admin), o backend cria um `mission_cost` (category "Combustível", cost_type "expense") vinculado à OS ativa daquela viatura. O Grid e DRE leem custos via `getMissionCostsByOS()` — fonte única de verdade. O `vehicleFuelCache` (financial_transactions) é apenas fallback quando não há mission_costs. Startup sync (`syncFuelingMissionCosts`) garante que fuelings anteriores sejam backfillados.
- **Custo de abastecimento é persistente.** O Grid/DRE deve mostrar TODOS os abastecimentos da viatura durante a missão (soma total), não apenas o mais recente. O valor deve permanecer no DRE da missão até que ela seja finalizada, independente da virada de data à meia-noite. NUNCA filtrar combustível por `due_date = TODAY()`.
- **Pedágio com missão = Custo + Receita (Reembolso).** Se `service_order_id` tiver valor, o sistema cria AMBOS os registros (expense + revenue) em `mission_costs` e `financial_transactions`. Impacto zero no lucro.
- **Pedágio sem missão (Vazio) = apenas Despesa.** Se `service_order_id` for null, o valor entra apenas como despesa da viatura (abate o lucro direto). Categoria: "Custos Fixos/Deslocamento Extra".
- **Valores financeiros são imutáveis após gravação.** Uma vez que um custo foi atribuído a uma missão, ele deve ser gravado como valor fixo. O sistema não deve recalcular ou filtrar esse valor pela data atual do servidor.

## System Architecture
The system employs a modern web stack: React with TypeScript and Vite for the frontend, and Express with Supabase Auth for the backend. **Supabase (PostgreSQL) is the ONLY database**. Tailwind CSS is utilized for styling, maintaining a professional monochrome aesthetic (black/white) with Montserrat/Inter typography. UI components adhere to an "Enterprise UI" design, ensuring a consistent and polished user experience.

**Key Architectural Decisions & Implementations:**
-   **Authentication & Authorization:** JWT-based authentication via Supabase Auth, implementing robust Role-Based Access Control (RBAC) through a `perfis_acesso` table. Session cookies are not used.
-   **Data Layer:** Two access paths to the SAME Supabase PostgreSQL:
    1. `storage.*` (in `server/storage.ts`) — uses Supabase REST API (`supabaseAdmin`) with `toSnakeObj()`/`toCamelObj()` converters. Returns camelCase TypeScript types.
    2. `db.*` (in `server/routes.ts`, `server/db-init.ts`) — uses Drizzle ORM connected to `SUPABASE_DATABASE_URL`. Returns camelCase via schema mapping.
    3. `supabaseAdmin.from(...)` — direct REST API calls in CRON and some routes. Returns snake_case.
-   **Operational Grid:** A central feature providing real-time vehicle tracking, status monitoring (speed, idle, signal loss), and integration with risk management companies.
-   **Mobile Interface for Field Agents:** A dedicated mobile-first experience for `funcionario` roles, supporting mission workflows, checklists, GPS tracking, and photo capture.
-   **Smart OCR Integration:** Utilizes OpenAI Vision for intelligent data extraction from documents (CNH, CNV, weapon registrations) to auto-fill forms.
-   **Vehicle Tracking Integration:** Real-time vehicle positions and telemetry are sourced from the TrucksControl / NewRastreamentoOnline SOAP webservice.
-   **API Design:** All API endpoints are defined in `server/routes.ts`, covering comprehensive CRUD and operational functionalities across various entities.
-   **UI/UX Design System:** A professional black/white aesthetic, specific typography, and standardized UI elements ensure visual consistency. The `DIRETORIA` role is visually highlighted.
-   **Google Maps Integration:** Used for location services, autocomplete, and mapping in the operational grid and escort calculation.
-   **Automated Tasks:** Cron jobs manage fleet monitoring and HR compliance checks.
-   **Login Selfie Enforcement:** `funcionario` users must take a selfie upon login, enforced by a protected route and stored in the `login_selfies` table.
-   **Driver Control Module (Controle de Condutor):** Real-time driver rotation tracking for escort vehicles. Tables: `driver_sessions` (session per vehicle operation) and `driver_shifts` (individual driving turns within a session). Supports start/swap/end flow with KM tracking, 2h driving alerts, and per-driver time summaries. Admin page: `/admin/controle-condutor` (OPERAÇÕES > Frota > Controle Condutor). Mobile page: `/mobile/controle-condutor`. API: `server/routes/driver-control.ts`.
-   **Audit System:** Comprehensive tracking of vigilante actions, page views, and security events (e.g., screenshot attempts, tab visibility changes) with an admin dashboard for monitoring and alerting.
-   **Financial Module:** Manages accounts payable/receivable with transaction CRUD, installment support, reporting, and category breakdown. **Automatic Financial Integration:** `financial_transactions` now has `origin_type` (manual/escort_billing/fueling/maintenance/mission_cost) and `origin_id` columns. Auto-transactions are created when: escort billing is APROVADA (INCOME), fueling is registered (EXPENSE), maintenance is registered with cost (EXPENSE), mission costs are added (EXPENSE). Auto-transactions are removed when the source record is deleted. Auto-transactions are locked — cannot be edited or deleted manually via the API or frontend. The Relatório tab includes a "Saldo Realizado" card and auto-transaction count. API: `GET /api/financial/resumo` returns comprehensive breakdown by origin type.
-   **Escort Calculation Engine:** Manages escort billing, client-specific price tables, frequent routes, and generates numbered Mission Bulletins (Boletim de Missão) with detailed calculation rules (KM, minimums, nocturnal/hazardous additions).
-   **OS Logic (Ordem de Serviço):** Implements the complete OS processing flow:
    - **Status Flow:** Funcionário fills OS data → status "A_VERIFICAR" → Administrativo reviews → "APROVADA" (auto-generates BO) or "REJEITADA" (solicita correção com motivo).
    - **Horário de Início Rule:** `Inicio_Missao = max(Horario_Agendado, Horario_Chegada_Real)` — if vigilante arrives early, scheduled time is used; if late, real arrival time is used.
    - **Franchise-Based Billing:** KM excedente is calculated as `max(0, KM_Carregado - Franquia_Cliente)`. Value breakdown shows franchise vs excess KM charges.
    - **Auto-Submit on Mission Close:** When a mission reaches "encerrada" status, the system automatically creates an `escort_billings` record with status "A_VERIFICAR", pulling KM from mission photos and times from mission lifecycle.
    - **Admin Review Panel:** In Financeiro > Boletim Medição tab, pending OS ("A Verificar") are displayed with full summary (horário considerado, horas trabalhadas, KM, franquia, excedente, valor total) and Aprovar/Rejeitar buttons.
    - **API Endpoints:** `POST /api/escort/billings/submit-os` (mobile submit), `POST /api/escort/billings/:id/revisar` (admin review), `GET /api/escort/billings/pendentes` (pending list).
    - **DB Columns Added:** `horario_agendado`, `horario_inicio_considerado`, `horas_trabalhadas`, `km_franquia`, `km_excedente`, `valor_franquia`, `valor_km_extra`, `revisado_por`, `revisado_em`, `motivo_rejeicao`, margin/result fields.
-   **Client "Pasta" System:** `/admin/calculo-escolta` is a client-centric management page. Selecting a client opens their "pasta" with 6 tabs: **Veículos** (client vehicles with CRUD + auto-registration from missions), **Tabelas** (price tables + frequent routes per client), **Contratos** (service contracts with validity/signature control), **Relatório de Missões** (completed missions with period filter), **Relatório de Faturamento** (billing summary with open vs closed boletins), and **Homologação** (document dispatch for client onboarding).
-   **Homologação Module:** Allows sending professional homologation emails to clients with selected company documents (Alvará, Cartão CNPJ, Contrato Social, Certificado de Registro PF, Seguro Responsabilidade Civil, Certidões Negativas). Features: company document upload/management (`company_documents` table), selectable document checkboxes, optional Apresentação Institucional and Tabela de Valores, SMTP configuration (env vars or per-request), HTML email template with Torres branding, and send history log (`homologation_logs` table). Uses `nodemailer` for email dispatch. API: `GET/POST/DELETE /api/company-documents`, `GET /api/homologation-logs/:clientId`, `POST /api/homologation/send`, `GET /api/email-config`.
-   **Client Vehicles (`client_vehicles`):** Table tracks vehicles associated with each client. Plates are unique per client. Auto-upsert: when a field agent fills the escorted vehicle plate during a mission, the plate (and driver info) is automatically registered/updated in `client_vehicles`. Full CRUD via `GET/POST /api/clients/:id/vehicles`, `PATCH/DELETE /api/client-vehicles/:id`.
-   **Service Contracts:** PDF generation via `GET /api/service-contracts/:id/pdf` (pdfkit) — produces a professional A4 contract document with black gradient headers, company branding (Torres Vigilância Patrimonial, CNPJ 36.982.392/0001-89), structured clauses (Partes, Objeto, Serviços/Equipamentos, Vigência, Reajuste, Valores from escort_contracts price table, Penalidades, Foro), signature blocks (Contratada/Contratante), witness section, and page footers. Download button (FileDown icon) on each contract card in the Contratos tab. Full CRUD for `service_contracts` table with vigência (padrão 1 ano determinado), reajuste (periodicidade/índice/cláusula), renovação automática, testemunha 1 & 2 (nome/RG/CPF/telefone), contratante email/telefone, objeto (readonly), data assinatura/registro. Contract blocking: clients without an active contract cannot access other tabs (Veículos, Tabelas, Missões, Faturamento).
-   **Employee "Pasta" System:** `/admin/funcionarios` features a unified "Pasta do Funcionário" view. Clicking any employee row opens their full folder with 8 tabs: **Documentos** (file uploads with expiry tracking — expired/expiring-soon badges), **Contrato** (employee data summary + contract generation/printing), **Multas** (traffic fines CRUD), **Disciplinar** (warnings/suspensions CRUD), **Faltas** (absences/medical certificates CRUD), **Ponto** (timesheets CRUD + Excel export), **Holerite** (payslips CRUD), **Salários** (salary history with create/delete). Replaces the previous scattered modals (DocumentsModal, HRDialog, SalaryModal) with a single consolidated view. The old modals remain in the file but are unused.
-   **Gestão de Holerites:** Dedicated payroll management page at `/admin/holerites` (sidebar: Funcionários > Holerites, adminOnly). Features: (1) **Smart Suggestion** — selecting employee+month auto-pulls salary base (CCT R$2.432,50), periculosidade (30%), overtime hours/value from timesheets, adicional noturno calculation from night shifts, and existing discounts from `employee_salary_discounts`; (2) **Composição de Valores** — editable fields for Salário Base, Periculosidade, Horas Extras, Adicional Noturno, Benefícios, Descontos with live Bruto/Líquido totals; (3) **Payment Flow** — Status (Pendente/Agendado/Pago) with Data de Pagamento; marking as "Pago" auto-creates EXPENSE in `financial_transactions` (origin_type: "holerite") feeding the DRE/Balanço Gerencial as RH cost; (4) **Comprovante Upload** — PDF/image stored as Base64 in `document_url`, downloadable per holerite; (5) **Relatório por Funcionário** — annual report dialog showing 12-month grid with bruto/líquido/status per month and year totals; (6) **Security** — invisible to Agente profiles, only Admin/Diretoria can access. Schema: `employee_payslips` table with `salario_base`, `horas_extras`, `adicional_noturno`, `periculosidade`, `beneficios`, `descontos`, `status`, `data_pagamento`, `financial_transaction_id`. APIs: `GET /api/payslips`, `GET /api/payslips/suggestion`, `POST /api/employees/:id/payslips`, `PATCH /api/payslips/:id`, `DELETE /api/payslips/:id`, `GET /api/payslips/employee-report/:id`, `POST /api/payslips/ocr`. (7) **Importação via OCR** — "Importar Holerite" button in the Novo Holerite dialog uploads a PDF/image, sends to OpenAI Vision (gpt-4o-mini) which extracts salário base, periculosidade, horas extras, adicional noturno, benefícios, descontos, mês/ano, employee name/CPF. System auto-matches employee by CPF or name fuzzy match. Fields are pre-filled for user review before saving. Audit events: CRIAR_HOLERITE, ATUALIZAR_HOLERITE, EXCLUIR_HOLERITE.
-   **Folha de Ponto Excel Export:** Server endpoint `GET /api/employees/:id/folha-ponto-excel?month=X&year=Y` generates a `.xlsx` file following the "Ficha Individual - Art 74/3 CLT" format (Portaria 3082 de 11/04/98). Auto-fills company header (Grupo Torres Patrimonial), employee data (matrícula, nome, cargo), daily timesheet entries (entrada, almoço, saída, horas extras), monthly totals, faltas (justificadas/não justificadas), folgas, suspensões, advertências, and signature blocks (colaborador + supervisor). Editing timesheet records is restricted to Diretoria and Admin roles only — other roles see a read-only view.
-   **Mission Timeline (`step_logs`):** JSONB column on `service_orders` records each step completion with timestamp, agent name/ID, and GPS coordinates. Visible as an expandable "Linha do Tempo" in both admin and mobile mission views. Each entry links to Google Maps for GPS verification.
-   **Offline Queue (v2 — Resilient):** Mobile agents can capture photos and advance steps even without signal. Actions are stored in `localStorage` via `client/src/lib/offlineQueue.ts`. Key specs: max 12 retries with exponential backoff (5s→30s + jitter), immediate flush on enqueue (800ms), reactive `subscribeQueue` pattern with edge-triggered `flushResult.eventId` to prevent duplicate toasts, manual "Reenviar" retry button in the syncing banner, `isNetworkError` catches 502/503/504 (deploy window), `forceFlush()` exposed for manual trigger.
-   **Balanço Gerencial:** Dedicated financial dashboard at `/admin/balanco-gerencial` providing comprehensive operational profitability analysis. Features: period navigation (daily/weekly/monthly/quarterly/semester/annual) with forward/back controls; KPI cards (faturamento, custos operacionais, lucro bruto, margem %); 4 tabs — **Balanço** (daily revenue vs costs bar chart, top vehicles/agents ranking), **Viaturas** (per-vehicle faturamento with R$ 35k/month meta progress bars, lucro/margem breakdown), **Agentes** (per-agent performance with same meta tracking, VRP paid vs revenue), **Missões** (detailed table per mission with boletim, client, route, viatura, agente, faturamento, custo, lucro, margem %, KM). API endpoint `GET /api/financial/dashboard` aggregates `escort_billings` + `financial_transactions` data. Accessible via sidebar Financeiro > Balanço Gerencial (adminOnly).
-   **Boletim de Medição (Separate Page):** Moved from Financeiro tab to its own dedicated page at `/admin/boletim-medicao`, accessible via sidebar menu item below "Ordens de Serviço". Displays completed OS grouped by client with data pre-filled from OS records (KM from mission photos, plates, agents, times). Includes filter buttons (Todas/A Verificar/Aprovadas/Rejeitadas), client-grouped accordion view with per-client totals, detailed modal with full OS breakdown, and approve/reject workflow. Only pedágios remain as extra expense (pernoite/gasolina/combustível removed). The Financeiro page now has 5 tabs (Pagar, Receber, Conferência, Relatório, Fechamento).
-   **Fueling Control (Controle de Abastecimento):** Full dashboard at `/admin/fueling` with rigorous fuel consumption tracking. Features: auto-sync vehicle KM on fueling registration (monotonic KM validation — rejects lower values), automatic KM recalculation from fueling history on delete/edit, full-tank-aware consumption calculation (partial fill-ups are accumulated until next full-tank for accurate km/L), per-vehicle expandable consumption cards showing history with km/L per interval and R$/km, 6 KPI stat cards (total fuelings, liters, cost, avg km/L, cost/km, best average), filter by vehicle and month, alert system for abnormally low consumption (< 6 km/L), auto-calculation of total cost from liters × unit price. Schema: `vehicle_fueling` table with `full_tank` boolean and `receipt_photo` fields.
-   **TicketLog Integration (Recolha Autônoma):** Integração com a API TicketLog para validação de abastecimentos em postos credenciados. Módulo: `server/ticketlog.ts`. Rotas: `GET /api/ticketlog/status` (verifica configuração), `POST /api/ticketlog/buscar-autorizacao` (busca código de autorização com dados do abastecimento), `POST /api/ticketlog/consultar-nfe` (consulta dados para emissão de NF-e), `POST /api/ticketlog/upload-nfe` (envia XML da NF-e). Autenticação: HTTP Basic Auth via `TICKETLOG_USER` + `TICKETLOG_PASS` (env vars). Ambiente: `TICKETLOG_ENV=producao|homologacao`. Colunas adicionadas em `vehicle_fueling`: `ticketlog_autorizacao`, `ticketlog_status` (autorizado/nfe_consultada/nfe_enviada/erro), `ticketlog_nfe_data` (JSONB), `ticketlog_codigo_estab`. UI: painel TicketLog no detalhe de cada abastecimento + coluna "TLog" na tabela de histórico mostrando ícone de status.
-   **Mission Geofences (Raio de 1km):** When a vehicle has an active mission (OS vinculada), the operational grid map automatically draws 1km radius circles at 4 key points: origin (blue), destination (red), agent 1 residence (purple), agent 2 residence (green). Coordinates are captured via Google Places Geocoder when addresses are selected in the PlacesAutocomplete component. Schema additions: `service_orders` has `origin_lat/lng`, `destination_lat/lng`; `employees` has `address_lat/lng`. The circles render on every map refresh and are cleared when the mission is completed. Each circle has a colored dot marker with tooltip showing the label and OS number.
-   **Rodízio Alert (Alerta de Rodízio):** Automated cron job sends "ATENCAO, RODIZIO DESSE VEICULO HOJE" text message via TrucksControl to vehicles whose plates match São Paulo rodízio rules for the day. Runs Mon-Fri at 06:30 and 16:30 BRT (09:30/19:30 UTC). Rodízio map: Mon=1,2; Tue=3,4; Wed=5,6; Thu=7,8; Fri=9,0. Uses `getVehicleCache()` to get veiIDs and plate numbers, then `sendCommand()` with `mensagem_texto`. Skips weekends automatically.
-   **Idle Alert Automation (Aviso de Cabine):** Automated system that sends cabin alerts (aviso de cabine) and text messages to vehicles when they are detected as idle (motor ligado, parado). When telemetry detects a vehicle has been idle for 5+ minutes: (1) sends `aviso_cabine_on` command via TrucksControl API (cmd=5), (2) sends `mensagem_texto` (cmd=4) with "Motor Ligado com carro parado .. desligue o veiculo!", (3) sets 2-minute timer to auto-disable cabin alert. When the vehicle's ignition turns off, the cabin alert is immediately disabled and the timer canceled. Anti-spam: only one alert per vehicle until ignition off. Manual commands also available in the operational grid command dialog (Bell/BellOff for cabin, MessageSquareText for free-text messages). Uses `processIdleAlert` and `processIgnitionOff` from `truckscontrol.ts`, triggered by `telemetry-engine.ts`.

-   **Mission Report PDF (`/api/service-orders/:id/relatorio-missao`):** Complete internal mission report with: OS data, team/vehicle details, armament, escorted vehicle, KM (saída/final/total/retorno), base return status, full step-by-step chronology with GPS coordinates, agent field updates with photos, and comprehensive photographic registry (all mission photos arranged in 2-column grid with GPS/KM/timestamp metadata). Accessible via green FileText button on service-orders page for completed/in-progress missions.
-   **Mission Operational Costs:** Per-mission cost tracking via `mission_costs` table (id, service_order_id, category, description, amount, cost_type, vehicle_id, employee_id, photo_url, latitude, longitude, created_at). `service_order_id` is nullable (for Pedágio Vazio). Categories: Pedágio, Combustível, Alimentação, Hospedagem, Estacionamento, Manutenção Emergencial, Outro. UI: "Custos Operacionais" section in OS edit form shows itemized list with add/delete, running total in header. API: `GET/POST /api/service-orders/:id/costs`, `DELETE /api/service-orders/:id/costs/:costId`. Scoped delete validates cost belongs to the OS.
-   **Pedágio Vazio (Empty Toll):** Allows agents to register toll expenses when NOT on an active mission (no OS). Mobile page at `/mobile/pedagio` with camera capture (receipt photo), BRL amount input, GPS location. API: `POST /api/mobile/pedagio-vazio` — resolves vehicle via last `vehicle_assignments` entry (or active OS fallback), creates `mission_cost` with `service_order_id=null`, creates `financial_transaction` as "Custos Fixos/Deslocamento Extra" (EXPENSE). Grid integration: vehicle-only costs are loaded and added to `custo_pedagio` for the first OS on that vehicle. Supabase Realtime subscription auto-refreshes grid/financial caches on INSERT events.
-   **Pedágio com Missão (Inline Toll Reimbursement):** Amber "+ Lançar Pedágio" button in `TransitStepView` (visible during `em_transito_origem`, `em_transito_destino`, `chegada_destino` steps). Opens inline form (amount + camera + GPS) within the mission screen — agent stays on mission page. API: `POST /api/mobile/pedagio-missao` — validates employee is assigned to OS and OS is active, creates BOTH expense (`mission_cost` category "Pedágio") AND revenue (`mission_cost` category "Reembolso de Pedágio") records with matching `financial_transactions` (EXPENSE + INCOME). Atomic: if revenue insert fails, expense is rolled back. Net DRE impact = zero (increases gross revenue and costs equally). Authorization: only assigned agents can register tolls on their missions.
-   **Automatic Toll Calculation:** When creating/editing an OS, the system automatically calculates tolls using Google Routes API v2 (`POST /api/calculate-tolls`). Sends `extraComputations: ["TOLLS"]` for the origin→destination route, receives toll estimates for 2-axle vehicles (Polos), and multiplies by 2 for round trip (ida+volta). Auto-fills the `pedagioEstimado` field on the OS and shows toll count/value in the route info display. On OS creation, if `pedagioEstimado > 0`, auto-creates a `mission_cost` entry (category "Pedágio") and corresponding `financial_transaction` (EXPENSE). The operational grid also uses `pedagioEstimado` as fallback if no mission_costs are registered. The Cotação de Gasto page also auto-calculates tolls when origin/destination are set. Schema: `service_orders.pedagio_estimado` (real column).
-   **Early Start Control:** Missions scheduled >30min in the future require admin authorization for early start. Backend blocks advance at `aguardando` step. Admin approves via amber Clock button in operational grid. Schema: `service_orders.early_start_approved`.
-   **Auto-Billing Fix:** Billing record creation no longer requires `kmFinal > kmInicial` — always creates `A_VERIFICAR` billing on mission close.
-   **Persistence Reform (Reforma de Persistência):**
    - **Immutable Cost Columns:** `service_orders` now has frozen financial columns (`fat_calculado`, `custo_combustivel_alocado`, `custo_pedagio_alocado`, `custo_pagamento_alocado`, `custo_outros_alocado`, `custo_total_alocado`, `lucro_calculado`, `margem_calculada`, `horas_missao_calculadas`, `km_total_calculado`, `custos_congelados_em`, `custos_congelados_por`). Once a mission is completed/closed, these values are **frozen** and never recalculated — the grid reads the frozen values directly.
    - **SQL RPC Functions:** `get_daily_fleet_summary(p_date)` and `get_fleet_totals(p_date)` Supabase RPC functions deliver pre-aggregated financial data from the database, eliminating frontend calculation drift. API: `GET /api/fleet-summary?date=YYYY-MM-DD`.
    - **Financial Audit Logs:** `financial_audit_logs` table records every financial change (UPDATE/DELETE) with `target_table`, `target_id`, `field_name`, `old_value`, `new_value`, `changed_by`, `changed_by_id`, `reason`, `metadata`, `created_at`. Instrumented on: `financial_transactions` (edit/toggle-status/delete), `vehicle_fueling` (edit/delete). API: `GET /api/financial/audit-logs?target_table=X&target_id=Y&limit=N`.
    - **System Audit Logs (Blindagem de Banco):** `system_audit_logs` table records every critical billing/faturamento action: `APROVAR_MISSAO`, `REJEITAR_MISSAO`, `REABRIR_MISSAO`, `GERAR_FATURA`, `EDITAR_MEDICAO`. Each entry has `user_id`, `user_name`, `user_role`, `action`, `target_id`, `target_type`, `details`, `ip_address`, `created_at` (TIMESTAMPTZ). Helper: `server/audit.ts` → `logSystemAudit()`. API: `GET /api/system-audit-logs?limit=N`. Approval also updates `service_orders.status` to `concluida`; reopen reverts to `em_andamento`. Schema: `shared/schema.ts` → `systemAuditLogs`.
    - **Edição Gerencial (Boletim de Medição):** Each OS row in the billing table has a detail sub-row showing billing breakdown (Acionamento, Franquia, KM Excedente, Hora Extra, Pedágio, etc.). Pencil button toggles inline editing mode with editable fields: `fat_acionamento`, `km_inicial`, `km_final`, `despesas_pedagio`, `horario_inicio`, `horario_termino`, `receitas_os`. "Salvar Medição" button sends `PATCH /api/escort/billings/:id/salvar` with `recalcular: true` which re-runs `calcularEscolta()` server-side using the contract, updates all derived values (km_excedente, fat_km, fat_hora_extra, fat_total, resultado_bruto/liquido), syncs `service_orders.fat_calculado`, and logs `EDITAR_MEDICAO` audit event. Editing allowed for statuses other than FATURADO/PAGO.
    - **Controle de Ciclo Financeiro:** Each client can have a billing cycle configuration: `billing_cycle` (por_missao/quinzenal/mensal), `payment_terms_days` (D+N days for due date calculation), `billing_cutoff_day` (day of month for closing). Client form has a "Ciclo Financeiro" section. Boletim de Medição shows cycle badge per client group (e.g., "Quinzenal · D+15"). "Gerar Fatura" dialog auto-suggests due date based on client's `payment_terms_days`. Daily CRON job (03:00 BRT) checks for approved OS that missed billing cutoff deadlines and creates `billing_alerts` (ATRASO_FATURAMENTO / PENDENTE_FATURAMENTO). Alert banner shows at top of Boletim page with resolve button. Table: `billing_alerts` (client_id, alert_type, message, os_numbers, period_start/end, resolved). API: `GET /api/billing-alerts`, `PATCH /api/billing-alerts/:id/resolve`, `GET /api/clients/:id/billing-config`. Client list table shows cycle column.
    - **30-Second Auto-Sync:** Operational grid, vehicle tracking, and financial transactions refetch every 30 seconds (`refetchInterval: 30000`) to prevent stale "ghost" data.
    - **Fueling KM Validation:** Both admin and mobile fueling endpoints reject KM entries with >1500km jump from current odometer, preventing data entry errors that distort consumption averages.
-   **Mission Updates & Agent Reports:** Multi-step update flow for field agents: (1) photo capture, (2) message with smart context-aware suggestions (e.g., "Chegada na Origem" only shows within 2km geofence), (3) send to admin. Photos stored as base64 data URLs in `mission_updates.photo_url` (validated server-side, max 5MB, must be `data:image/*`). Operational grid shows agent's last update message/step in blue badge. "Atualização Recente" button on vehicle cards copies a formatted report template (WhatsApp-style with emojis: OS number, status, date/time, client, origin/destination, driver info, agents, progress %, occurrence, location, Google Maps link) to clipboard along with the agent's photo. `mission_updates` table: `id, service_order_id, os_number, employee_id, employee_name, message, mission_step, latitude, longitude, photo_url, read_by_admin, created_at`. Alert banner polls unread updates every 15s. State preserved on send failure for retry.

-   **Mobile Folha de Ponto (Clock In/Out):** Full time tracking from mobile at `/mobile/ponto`. 4-step process: Entrada → Saída Almoço → Retorno Almoço → Saída. Each step requires photo capture (front camera) and GPS location. Data stored in `employee_timesheets` with `clock_in_photo/lat/lng`, `clock_out_photo/lat/lng`, `lunch_out_photo/lat/lng`, `lunch_in_photo/lat/lng` fields. API: `GET /api/mobile/ponto/today`, `POST /api/mobile/ponto/clock`.
-   **Mobile Abastecimento (Fueling):** Mobile fueling registration at `/mobile/abastecimento`. Requires 3 mandatory photos: bomba, NF, hodômetro. Enter KM, liters, R$/litro, posto. Auto-calculates total. Validates KM > current vehicle KM. Updates vehicle KM on submit. Returns oil change alert if applicable. **Financial sync (3-way linkage):** on successful registration: (1) `createAutoTransaction()` creates a `financial_transaction` (EXPENSE, category "Combustível", origin_type "fueling") for financial dashboard; (2) if the vehicle has an active OS, creates a `mission_cost` record linked to that OS (category "Combustível", cost_type "expense") so the DRE sees the fuel cost immediately; (3) the DRE detail endpoint prevents double-counting — when `mission_costs` has fuel entries, the `financial_transactions` fueling lookup is skipped. Frontend calls `invalidateRelatedQueries("vehicle"/"financial"/"mission-cost")` which cascades to all related query keys AND propagates to other open tabs via BroadcastChannel. API: `GET /api/mobile/abastecimento/vehicle`, `POST /api/mobile/abastecimento`. Schema additions: `vehicle_fueling.pump_photo`, `vehicle_fueling.odometer_photo`, `vehicle_fueling.latitude/longitude`, `vehicle_fueling.gasoline_price`, `vehicle_fueling.ethanol_price`, `vehicle_fueling.fuel_recommendation`, `vehicle_fueling.recommendation_followed`.
-   **Mobile Ocorrências (Incident Reports):** Mobile incident reporting at `/mobile/ocorrencia`. Types: Acidente, Quebra Mecânica, Avaria, Manutenção, Segurança, Outro. Supports up to 5 photos + description + GPS. History tab shows past reports with admin responses. API: `GET/POST /api/mobile/ocorrencias`, `GET /api/ocorrencias` (admin), `PATCH /api/ocorrencias/:id` (admin). Table: `employee_occurrences` (employee_id, vehicle_id, type, description, photos[], lat/lng, status, admin_notes).
-   **Ponto Operacional (Long Duration Time Clock):** Extended time tracking for field agents on multi-day trips. Mobile page at `/mobile/ponto-operacional` allows agents to open a shift and keep it running indefinitely (across days) until they click "Finalizar Ponto". Features: live chronometer, entry/exit with optional notes, monthly accumulated hours display. Admin dashboard at `/admin/ponto-operacional` (sidebar: Funcionários > Ponto Operacional) shows all agents with: progress bar (green < 190h, amber 190-220h, red > 220h), "Em Viagem" badge for agents with open shifts, expandable per-agent history with individual records. 220h monthly limit rule: hours above 220h classified as overtime with 50/50 split (50% payroll, 50% employee bonus). Salary base: R$ 2,432.50. Table: `ponto_operacional` (Supabase-only, UUID PK) with employee_id, employee_name, entrada, saida, horas_decimal, status (aberto/fechado), observacao. API: `GET /api/ponto-operacional/aberto`, `POST /api/ponto-operacional/entrada`, `POST /api/ponto-operacional/saida`, `GET /api/ponto-operacional/resumo-mensal?mes=YYYY-MM`, `GET /api/ponto-operacional/historico/:employeeId`, `DELETE /api/ponto-operacional/:id`.
-   **Oil Change Alert:** Vehicles track `last_oil_change_km`. System warns at 9,000 km since last change and alerts at 10,000 km. Alerts shown on fueling submission and via `GET /api/mobile/oil-alert/:vehicleId`.
-   **Reference Points (Pontos de Referência):** Admin can register named geographic reference points on the map via right-click context menu on any vehicle. Each point has a name, coordinates (from vehicle's current position), and configurable radius (default 500m). When any vehicle enters a reference point's radius, the "Referência" column in the vehicle table shows the point name. Reference point circles are drawn on the map with colored borders. Full CRUD via `GET/POST/PATCH/DELETE /api/reference-points`. Table: `reference_points` (id, name, latitude, longitude, radius_meters, color, created_at). Management dialog accessible from context menu.
-   **Vehicle Context Menu (Right-Click):** All vehicle actions moved from the "Ações" button column to a right-click context menu on vehicle table rows AND map markers. Actions: Localizar no Mapa, Visualizar Pré-Alerta, Espelhamento, Enviar Comando (bloquear/desbloquear/sirene/aviso cabine/mensagem), Retornar à Base, Liberar Início Antecipado, Copiar Relatório, Nova OS, Docs VTR, Finalizar Missão, Cancelar Missão, Cadastrar Ponto de Referência, Gerenciar Referências. The "Ações" column was replaced by "Referência" column.
-   **Extended Price Table (escort_contracts):** New fields: `valor_acionamento` (R$/KM acionamento), `franquia_horas`, `franquia_km`, `valor_hora_extra`, `valor_km_extra`, `valor_cancelamento`, `custo_deslocamento_100km` (rodando com saída da base). Displayed in client Tabelas tab with 2-row card layout.
-   **Agent Location Tracking (Rastreamento de Agentes):** Automatic background GPS tracking of field agents every 10 minutes. The mobile app (`MobileLayout`) requires GPS permission on login and sends position updates via `POST /api/agent/location`. Current positions stored in `agent_locations` (upsert per user). Full position history stored in `agent_location_history` with `(user_id, created_at)` index for efficient date queries. Admin panel at `/admin/tracker` (Rastreador in sidebar under Frota) shows real-time agent positions with online/offline status (15min threshold), Google Maps links, accuracy/speed data, and per-agent daily history dialog with date picker. Read endpoints (`GET /api/agent/locations`, `GET /api/agent/locations/:userId/history`) are admin-only (`requireAdminRole`). Auto-refresh every 60 seconds on admin panel.
-   **Mission Route Tracking (Rastreamento de Rota):** Records vehicle GPS positions during active missions (status `em_andamento`) in the `mission_positions` table. Positions are stored with 50m minimum distance filter using an in-memory map to avoid DB reads on each poll cycle. Endpoints: `GET /api/service-orders/:id/positions` (raw positions), `GET /api/service-orders/:id/route` (planned route polyline from Google Directions API + positions, cached in OS `route` field). Frontend: "Ver Rota" button in map info window and vehicle context menu. Draws planned route (black polyline), traveled route (blue polyline), start marker (green dot), last position marker (red dot), origin/destination arrows on the operational grid map. Floating legend bar with close button to dismiss. Uses `google.maps.geometry.encoding.decodePath` for polyline rendering. Window events (`show-route`) for cross-component communication.
-   **Route Progress Bar (Barra de Progresso GPS):** Visual progress indicator on grid cards and forward dialog showing vehicle position between origin and destination. Uses `getRouteProgress()` (haversine-based with 1.3x road multiplier) to calculate % complete and remaining distance. `RouteProgressBar` component renders a route line with car icon, origin (blue) / destination (red) dots, percentage, and "X km restantes" label. Compact variant on grid cards, full variant in forward/share dialog. Falls back to step-based `getMissionProgress()` when GPS coordinates unavailable. WhatsApp reports include `{{routeInfo}}` template variable with distance details.
-   **TrucksControl Resilience (Last Valid Position):** Three improvements to vehicle tracking reliability: (1) **Empty packet handling** — messages with lat=0/lon=0 are rejected from overwriting valid positions; system maintains `lastValidMessageByVehicle` and `lastValidMessageBySpy` maps as fallback. (2) **Timestamp ordering** — incoming messages sorted by tracker's `dt` timestamp (not API `mId`), preventing buffer dumps from reordering positions chronologically. (3) **Valid coord validation** — `hasValidCoords()` validates lat/lon are non-zero and within geographic bounds before storing. When a vehicle temporarily loses GPS, the grid continues showing the last known valid position instead of "Sem sinal".
-   **TrucksControl SWR + Fast Timeout:** API calls to TrucksControl webservice have 5s timeout (was 15s). `getCachedPositions()` implements Stale-While-Revalidate (SWR): when cache expires, stale data is served immediately while refresh runs in background. This prevents slow external API calls from blocking user requests.
-   **Performance Profiling:** Three-level profiling system: (1) `[SLOW]` — API routes >500ms logged with route/method/status/duration, stored in ring buffer (last 50). (2) `[SLOW-SUPA]` — Supabase fetch calls >500ms. (3) `[SLOW-SQL]` — Local PostgreSQL queries >200ms. Dashboard at `GET /api/health/slow-routes` (admin only) shows ranked summary with count/avg/max per route.

## Real-Time Data Synchronization
-   **Query Cache:** `staleTime: 30s` + `refetchOnWindowFocus: true` ensures data refreshes automatically when switching between pages/tabs.
-   **Centralized Invalidation:** `invalidateRelatedQueries(scope)` in `client/src/lib/queryClient.ts` provides cross-page cache invalidation. Scopes: `"vehicle"`, `"employee"`, `"billing"`, `"financial"`, `"service-order"`, `"mission-cost"`. Each scope cascades invalidations to all related queryKeys (e.g., changing a vehicle also invalidates dashboard, operational grid). **Supabase Realtime** channel (`realtime-sync-*`) listens for INSERT events on `mission_costs`, `financial_transactions`, `vehicle_fueling`, and `mission_updates` tables + UPDATE on `service_orders` to auto-invalidate caches across all open tabs/devices. Channel uses exponential backoff reconnection (up to 30s delay, max 10 retries). On `online` event, all channels are re-subscribed and queries invalidated.
-   **Financial Transaction Sync:** Billing approval creates financial transactions with `origin_type: "escort_billing"`. The approval flow removes any legacy `"service_order"` origin transactions before creating the correct one. Total = `fat_acionamento + fat_hora_extra + fat_km + despesas_pedagio`.

### Conexão Infalível (Agent App Resilience)
- **É proibido deslogar o usuário por inatividade ou expiração de token sem tentativa automática de renovação.** A integridade da comunicação entre App e Painel Admin é prioridade máxima.
- **Token de Longa Duração:** Supabase Auth emite access_token (~1h) + refresh_token (~30 dias). O app renova o token proativamente a cada 45 minutos via `supabase.auth.refreshSession()`. Se um request retorna 401, tanto `apiRequest` quanto `offlineQueue.flushQueue` tentam refresh antes de falhar.
- **Persistência Offline (v2):** Ações offline são enfileiradas em `localStorage` via `offlineQueue.ts`. Max 12 retries com backoff exponencial (5s→30s + jitter). Flush imediato ao enfileirar (800ms). Padrão reativo `subscribeQueue` com `flushResult.eventId` edge-triggered evita toasts duplicados. Botão "Reenviar" manual no banner de sincronização. `isNetworkError` expandido para 502/503/504 (janela de deploy).
- **Reconexão Agressiva:** Quando detectado offline, um watchdog tenta reconectar a cada 5 segundos (via `/api/health` ping). Ao reconectar: refresh do token + invalidação de todas as queries. Banner visual mostra "Reconectando..." com spinner.
- **WakeLock Keep-Alive:** `navigator.wakeLock.request("screen")` solicitado no mount do app do agente. Re-solicitado ao retornar de background (visibilitychange → visible). Previne o OS de dormir a tela durante missão.
- **Visibilitychange Re-ping:** Ao voltar para a aba/app, o agente faz imediatamente: ping `/api/health` + `supabase.auth.refreshSession()`. Se refresh falha, reporta para `POST /api/auth/token-failure`.
- **Token Failure Logging:** Backend `POST /api/auth/token-failure` registra falhas de refresh na tabela `token_failure_logs` (employee_id, employee_name, error_message, user_agent, ip_address, created_at). Permite auditoria de problemas de autenticação em campo.

### Ouvinte Imortal (Admin Grid Resilience)
- **Monitoramento de Conexão Admin:** O Grid detecta `online`/`offline` events e exibe banner vermelho "Tentando reconectar..." quando offline. Ao reconectar, faz invalidação total de queries (Fetch Imediato).
- **Supabase Realtime Push (Imortal):** Canal Realtime escuta `mission_updates`, `service_orders` (INSERT+UPDATE+*), `vehicle_fueling`, `mission_costs`, `financial_transactions`, `escort_billings`. Reconexão infinita (sem cap de retries), heartbeat a cada 60s que reconecta se stale >120s ou disconnected. Re-subscribe automático no `visibilitychange` (volta de background) e `online` event. Scope `mission-update` adicionado para invalidar grid+updates+service-orders em cascata.
- **Auto-refresh global:** Intervalo reduzido de 60s para 30s. Polling do grid operacional mantém 30s via `REFRESH_INTERVAL_MS`.
- **Payload otimizado:** `/api/mission/updates` e `/api/operational-grid` não retornam mais fotos base64 inline — substituem por `[has_photo]` + `hasPhoto: true`. Fotos carregam on-demand via `GET /api/mission/updates/:id/photo`. Resposta do grid caiu de ~5-20s para <1s.
- **Alerta VTR Offline (5min):** Se uma viatura com missão ativa não envia update por >5 minutos, o card dela muda para VERMELHO com badge "SEM SINAL Xmin" piscante e ícone WifiOff. Um alarme sonoro distinto (3 bipes curtos em onda quadrada, 660/440Hz) é disparado quando uma VTR recém-entra em estado offline. Debounce de 30s para evitar alarmes repetitivos.
- **Alerta VTR Crítico (15min):** Se uma viatura passa >15 minutos sem update, o card escala para vermelho intenso (ring-4, shadow-lg, bg-red-100) com badge "PERDA DE SINAL Xmin" + ícone WifiOff. Alarme crítico `playCriticalAlarm()` dispara 5 bipes (880/660/440Hz, volume 0.35) — mais alto e mais longo que o alarme padrão. Debounce de 30s independente.
- **Som de Notificação:** Beep curto (880Hz sine wave) ao receber qualquer nova atualização de agente. Toggle persistido em localStorage.

-   **Selo de Auditoria (Copy Audit Stamp — Produtividade Livre):** Copy buttons ("Copiar Texto", "Copiar Foto") ALWAYS execute the copy immediately — never block, never show `alert()` or `confirm()`. The stamp "(Copiado por: [Nome] às [DD/MM HH:MM])" is purely informational, displayed below the copy buttons. Each click: (1) copies to clipboard first, (2) shows "Relatório Copiado!" toast, (3) fires `POST /api/mission/updates/:id/copy-audit` in background to update stamp. Stamp syncs in real-time via Supabase Realtime UPDATE listener. The audit log is for history only and must NEVER impede subsequent clicks. Schema: `mission_updates.copiado_por` (text), `mission_updates.copiado_em` (timestamp).

### Segregação de Funções (Security Level 1)
- **Barreira Web para Agentes:** `ProtectedRoute` em `App.tsx` verifica `user.role === "funcionario"` e redireciona para tela "Acesso Negado" (`access-denied.tsx`). Agentes só acessam `/mobile/*`. Login por CPF no `/admin` redireciona automaticamente para `/mobile`.
- **Blindagem Financeira (API):** Todas as rotas `/api/financial/*`, `/api/escort/billings/*`, `/api/escort/contracts`, `/api/employees/:id/salary-*` exigem `requireAdminRole` (role `admin` ou `diretoria`). Agentes recebem 403 Forbidden.
- **Omissão no App Mobile:** App de abastecimento mostra apenas dados operacionais (litros, km, relação etanol/gasolina). Nenhum dado de faturamento, lucro, margem ou DRE é acessível via rotas mobile.
- **REGRA INVIOLÁVEL:** É terminantemente proibido exibir dados financeiros para perfis de agentes. O acesso web é restrito a administradores. Agentes operam exclusivamente via App Mobile.

### Chat Despacho Tático de Missões
- **Widget Flutuante:** Botão preto fixo no canto inferior direito (z-60) em AdminLayout e MobileLayout. Abre painel lateral com chat completo sem navegar. Badge com contagem de não lidas via `/api/chat/unread-count`.
- **Criação de Grupos:** Modal "Nova Conversa" permite selecionar múltiplos usuários e criar conversas tipo `group` com nome personalizado. Funciona em admin, mobile e widget.
- **Mensagem `mission_invite`:** Novo tipo de mensagem renderizado como card especial (fundo escuro, ícone Shield, dados da OS). Sem dados de cliente ou valores financeiros. Card exibe: número da OS, tipo, horário, origem, destino.
- **Enviar OS para Chat:** Botão Shield na área de input do chat admin. Abre seletor de OS disponíveis e envia `mission_invite` via `POST /api/chat/send-mission-invite`.
- **Aceite Tático:** Agente clica "Aceitar Missão e Termos de Conduta" no card. Backend `POST /api/chat/accept-mission` atualiza `mission_status` para "aceita", insere mensagem `system` confirmando, registra auditoria. Botão desaparece após aceite, mostrando "Missão aceita" com check verde.
- **Reflexo no Grid:** `mission_status: "aceita"` aparece no Grid Operacional como "Missão Aceita" com badge emerald (verde). Adicionado em `getStatusDisplay`, `getTransitStatus` e `getMissionProgress`.
- **Endpoint `GET /api/chat/service-orders-available`:** Lista OS disponíveis para envio (sem concluídas/canceladas/encerradas), filtrando dados sensíveis.

### Identidade Tática (Tactical Identity)
- **Reverse Geocoding:** `GET /api/reverse-geocode?lat=X&lng=Y` usa Nominatim (OpenStreetMap) para converter coordenadas GPS em endereço completo (rua, número, bairro, cidade/UF). Cache client-side em `_reverseGeocodeCache`.
- **Componente `ReverseGeocodeAddress`:** Exibe endereço completo com ícone `ShieldCheck` (verde esmeralda) + link "Ver no mapa". Usado no feed de notificações (MissionUpdatesAlert) e na AlertsTimeline.
- **Ícone Shield:** Nas atualizações de missão, `ShieldCheck` substitui `MapPin` para reforçar identidade tática/segurança patrimonial.
- **Nominatim Rate Limit:** 1 req/seg. Cache evita chamadas duplicadas. Endereços formatados: "Rua X, 123, Bairro, Cidade/UF".

### Otimizações de Memória e Performance (Backend)
- **Logging middleware (index.ts):** `capturedJsonResponse` é limpo após log (`= undefined`). Arrays logam apenas `[Array(N)]` em vez de serializar o conteúdo inteiro.
- **Maps com pruning automático:** `lastMissionPos`, `lastRecordedPos` (operational.ts), `speedAlertCooldown`, `idleAlertSent` (telemetry-engine.ts) possuem cap de 500/300 entradas com limpeza periódica.
- **`_roadDistCache` (service-orders.ts):** Cap de 200 entradas + TTL de 5min. Prune automático ao inserir.
- **`syncAllTables` (pg-fallback.ts):** Tabelas grandes (audit_logs, telemetry_events, mission_positions, chat_messages) limitadas a 5000 linhas no sync.
- **Billing backfill (routes.ts):** Usa batch loading (3 queries) em vez de N+1. Filtra apenas billings com campos null.
- **Startup deferral:** `syncMissingAutoTransactions` inicia após 10s, `syncFuelingMissionCosts` após 25s para não sobrecarregar o boot.
- **Endpoint `/api/health/memory`:** Retorna RSS, heap, uptime para monitoramento.
- **Maps duplicados removidos:** `lastMissionPos` e `lastRecordedPos` de `routes.ts` removidos (duplicatas das de `operational.ts`).

## External Dependencies
-   **Supabase:** Provides authentication (Supabase Auth) and PostgreSQL database hosting.
-   **OpenAI Vision:** Used for OCR capabilities in document processing.
-   **TrucksControl / NewRastreamentoOnline:** SOAP webservice for real-time vehicle tracking data, remote commands, and vehicle mirroring functionalities.
-   **BrasilAPI:** Utilized for CPF lookup functionality.
-   **Google Maps Platform:** Provides mapping services, Places API for autocomplete, and location functionalities.
-   **Asaas:** Gateway de pagamentos brasileiro para geração de boletos, PIX e cobranças. Integração via `server/asaas.ts`. Requer `ASAAS_API_KEY` (secret) e `ASAAS_API_URL` (env var, default produção). Webhook: `POST /api/asaas/webhook`.
-   **Framer Motion:** Used for frontend animations.
-   **Lucide React / React Icons:** Icon libraries for the user interface.

### Controle de Faturas (Módulo Asaas)
- **Tabela `invoices`:** Armazena faturas localmente com vínculo opcional ao Asaas (`asaas_payment_id`, `asaas_customer_id`).
- **Rotas API (`server/asaas.ts`):** `GET/POST /api/invoices`, `PATCH/DELETE /api/invoices/:id`, `POST /api/invoices/:id/sync`, `POST /api/invoices/:id/resend` (Asaas notification), `POST /api/invoices/:id/resend-email` (SMTP billing email), `GET /api/invoices/:id/pix`, `GET /api/asaas/status`, `GET /api/asaas/customers`, `POST /api/asaas/webhook`.
- **Todas rotas protegidas por `requireAdminRole`** (exceto webhook que é público para receber eventos do Asaas).
- **E-mail de Cobrança (SMTP):** Ao criar uma fatura, o sistema envia automaticamente e-mail via SMTP (Office 365) para o cliente com boleto/NF/PIX. Usa `email_financeiro` ou `email` do cadastro do cliente. Colunas `email_sent`, `email_sent_at`, `email_sent_to` na tabela `invoices` rastreiam o envio. Botão "Enviar Boleto/NF" / "Reenviar Boleto/NF" no detalhe da fatura (`/admin/faturas`). BCC automático para `thiago@grupotmseg.com.br` e `financeiro@torresseguranca.com.br`.
- **Frontend:** `/admin/faturas` — Painel de criação, visualização, sincronização, cancelamento e confirmação de pagamento de faturas. Indicador visual de e-mail enviado (ícone MailCheck azul) na listagem. Acessível via sidebar Financeiro > Faturas / Cobranças.
- **Funciona sem Asaas:** Se `ASAAS_API_KEY` não estiver configurada, faturas são registradas apenas localmente sem geração de boleto/PIX.

  ### Boletim — Bloqueio de Reenvio Duplicado ao Cliente
  - **Tabela `boletim_approvals`** ganhou colunas `sent_by` (TEXT) e `sent_by_user_id` (INTEGER) para registrar quem disparou o "Enviar para Cliente".
  - **Backend (`server/routes/boletim-approval.ts`):**
    - `POST /api/boletim/enviar-aprovacao` agora **rejeita com HTTP 409** quando já existe uma aprovação ativa (status `PENDENTE` ou `APROVADO`) cobrindo qualquer um dos billing_ids do mesmo cliente. A resposta inclui `existing` com `sentAt`, `sentBy`, `status`, `clientEmail` e `overlapBillingIds`. Para forçar reenvio basta enviar `force: true` no body.
    - Insere `sent_by` (= `req.user.name`) e `sent_by_user_id` no registro.
    - Novo endpoint `GET /api/boletim/approval-status?clientId&billingIds=1,2,3` devolve `{ active, recent[] }`: `active` é a aprovação não-recusada que cobre qualquer billing_id consultado; `recent` são as últimas 5 aprovações relacionadas.
  - **Frontend (`client/src/pages/admin/relatorio-faturamento.tsx`):**
    - Consulta `/api/boletim/approval-status` sempre que o relatório é gerado. Se houver envio ativo, exibe banner azul (PENDENTE) ou verde (APROVADO) com **data, hora, usuário e e-mail** do envio.
    - Botão "Enviar para Cliente" muda para "Aguardando cliente" (cinza) ou "Cliente aprovou" (verde) e exige confirmação dupla para forçar reenvio (envia `force: true` ao backend).

  ### Relatório de Notas Fiscais (`/admin/relatorio-nf`)
  - **Base unificada por `data_missao` (sem duplicidade):** o endpoint `GET /api/relatorio-nf?from=…&to=…` (`server/asaas.ts`) parte de `escort_billings` filtradas por `data_missao` no período (a mesma chave usada em `/api/escort/billings` e `/api/financial/dashboard`) e agrupa cada billing em **UMA única linha** seguindo a regra: (1) se a billing tem `invoice_id` apontando para uma `invoices` da Torres → linha `FAT-{invoice_id}`; (2) senão, se está em `billing_ids` de algum `boletim_approval` → linha `BOL-{ap_id}`; (3) senão → linha avulsa `BIL-{billing_id}` com status `AGUARDANDO_BOLETIM`. Isso elimina a antiga duplicidade entre invoice + boletim + OS órfã (que inflava o total). `clientName` é resolvido pelo cliente atual (`clients.name`), não pelo snapshot armazenado em billings/invoices.
  - **Status normalizado:** cada linha vem com `normalizedStatus` consolidado: `PENDENTE_APROVACAO` (boletim aguardando cliente), `AUTORIZADO` (aprovado/faturado sem NF), `NF_PROCESSANDO`, `NF_EMITIDA`, `NF_ERRO`, `NF_CANCELADA`, `PAGO`, `VENCIDO`, `AGUARDANDO_BOLETIM`. Helpers `normalizeInvoiceStatus`/`normalizeBoletimStatus` cobrem os múltiplos rótulos retornados pelo Asaas (ERROR/ERRO, CANCELED/CANCELLED, PROCESSING_CANCELLATION etc.).
  - **Cada linha mostra `osList`** (lista clicável de OS vinculadas que abre `/admin/service-orders?os={id}` com o modal da OS), permitindo navegar do faturamento para a operação.
  - **Sincronização periódica com Asaas:** cron `*/15 * * * *` em `server/cron.ts` chama `reconcileAllInvoicesAsaas({ limit: 80 })`, que para cada fatura com `asaas_payment_id` consulta `/payments/{id}` (status, paymentDate, invoiceUrl) e `/invoices/{id}` ou `/payments/{id}/fiscalInfo` (status NFS-e, número, URL do PDF). Pula faturas em estado terminal (`RECEIVED + AUTHORIZED/SYNCHRONIZED/CANCELED`) e atualizadas <8min, exceto quando `force=true`. Estado mantido em `nfReconcileState` (startedAt/completedAt/processed/updated/errors).
  - **Sync manual:** `POST /api/asaas/reconcile-all { force?, limit? }` dispara em background; UI faz polling em `/api/relatorio-nf`. `GET /api/asaas/reconcile-status` retorna o estado atual.
  - **Frontend (`client/src/pages/admin/relatorio-nf.tsx`):** 7 cards (Total / Aguard. cliente / Autorizado / NF Emitida / Pago / Com Erro / Cancelada) clicáveis para filtrar a tabela. Mostra a última sincronização Asaas com contadores de processadas/atualizadas/erros, botão "Sincronizar c/ Asaas" e exportação Excel via `exportFormattedExcel`.

### E-mail Marketing Automatizado (Leads)
- **Tabela `email_queue`:** Fila de e-mails com status (pendente → enviado → lido → erro), tracking_id para pixel de rastreamento, contadores de abertura.
- **CRON automático:** Dispara 5 e-mails a cada 10 minutos para evitar bloqueio do SMTP Office 365.
- **Pixel de rastreamento:** `GET /api/leads/pixel/:trackingId.png` — registra abertura de e-mail e atualiza lead para "morno" automaticamente.
- **Reply-To:** Todas respostas vão para `escolta@torresseguranca.com.br` e `diretoria@torresseguranca.com.br`.
- **Rotas API:** `POST /api/leads/enfileirar-todos` (enfileira em lote), `POST /api/leads/disparar-agora` (disparo manual), `GET /api/leads/email-stats` (estatísticas diárias), `GET /api/leads/email-queue` (fila completa), `POST /api/leads/email-queue/:id/marcar-respondido`, `POST /api/leads/email-queue/limpar-fila`.
- **Frontend:** Aba "E-mail Marketing" na página de Leads com 8 stat cards, gráfico de barras diário (enviados/abertos/respondidos), painel de controle de fila com filtros por status.
### Cancelamento/Recusa de OS — Motivo Obrigatório
- **Coluna `cancellation_reason` (TEXT)** em `service_orders` armazena o motivo informado pelo usuário ao mudar status para `cancelada` ou `recusada`.
- **Validação backend:** `PATCH /api/service-orders/:id` rejeita (HTTP 400) qualquer mudança para esses status sem `cancellationReason` (mín. 3 caracteres).
- **Validação frontend:** Form de edição da OS (`client/src/pages/admin/service-orders.tsx`) exibe Textarea obrigatório quando o status seleciona cancelada/recusada e bloqueia o salvamento sem motivo.
- **Componente reutilizável `CancelReasonBadge`** (`client/src/components/cancel-reason-badge.tsx`): ícone Info circular sobreposto aos badges de status, com tooltip mostrando motivo. Usado em Operational Grid, Lista de OS, Relatório de OS e Boletim de Medição.
- **Helper `promptCancellationReason`** (`client/src/lib/cancel-reason.ts`): pede o motivo via prompt nativo, repete até atender mínimo 3 chars.

### OMEGA — Nº do Processo no Boletim
- **Coluna `processo_omega` (TEXT)** em `service_orders` (campo opcional, exposto na UI somente quando o cliente possui "OMEGA" no nome).
- **No envio do boletim** (`POST /api/boletim/enviar-aprovacao`): coleta nº de processo de cada OS do período e:
  - Anexa ao **assunto do e-mail** (`— Processo X` ou `— Processos X, Y`).
  - Insere headers SMTP `X-Omega-Processo` e `References` com identificadores de processo.
  - Inclui no cabeçalho do XLSX (linha "REFERENTE AO SERVIÇO ...") como `— PROCESSO N` / `— PROCESSOS N, M`.

### Retenção de INSS na NF (IN RFB nº 2.110/2022)
- **Por que existe:** A atividade de vigilância/escolta armada se enquadra no Anexo IV da LC 123 e envolve cessão de mão-de-obra. Conforme o art. 111, II da IN RFB nº 2.110/2022, a contratante deve reter 11% sobre o valor do serviço e recolher diretamente à Previdência. O valor retido é abatido no DAS pela contratada (Torres). Quando o cliente está dispensado da retenção, a NF deve trazer a observação do art. 115 da mesma IN.
- **Configuração por cliente:** colunas `retem_inss` (BOOLEAN, default false) e `inss_aliquota` (NUMERIC(5,2), default 11.00) em `clients`. Editáveis em **Admin → Clientes** (toggle "Reter INSS na NF" + campo de alíquota), logo abaixo do toggle "Emite NF".
- **Persistência da retenção na fatura:** colunas `valor_inss_retido` e `inss_aliquota` em `invoices` guardam o valor calculado no momento da emissão (auditoria/histórico).
- **Emissão da NFS-e via Asaas (`server/asaas.ts`):**
  - `buildNfseInvoicePayload` e `buildFiscalPayload` aceitam `retemInss` + `inssAliquota` e populam `taxes.inss = aliquota` (Asaas trata como percentual).
  - As **observações da NF** trazem o texto legal: `Retenção de INSS sobre cessão de mão-de-obra (Anexo IV) — Art. 111, II da IN RFB nº 2.110/2022. Alíquota: X%. Valor retido: R$ Y,YY.` Quando `retem_inss=false`, é incluída a observação de dispensa do art. 115.
  - Aplicado tanto na emissão **individual** (`POST /api/invoices`) quanto na **consolidada por boletim** (`POST /api/boletim-medicao/gerar-fatura/:clientId`). Em ambos os casos, o `fiscalObservations` da cobrança no Asaas também recebe o texto.

### Comboio — Múltiplos Motoristas Escoltados (mobile)
- **Coluna `extra_drivers` (JSONB, default `[]`)** em `service_orders` armazena motoristas adicionais quando a OS é um comboio (formato: `[{name, phone, plate}]`). O motorista principal continua nas colunas `escorted_driver_name/phone/vehicle_plate` para preservar compatibilidade com relatórios, NFS-e e laudo.
- **App do agente — etapa `checkin_dados_motorista`** (`client/src/pages/mobile/missao.tsx`): card "Motorista 1 (principal)" + botão tracejado "Adicionar Motorista (Comboio)" cria slots adicionais com Nome/Telefone/Placa e botão "Remover". Validação: nome+placa obrigatórios em cada slot extra.
- **Endpoint `POST /api/mission/escort-data`** (`server/routes/mission.ts`): aceita `extraDrivers: [{name, phone, plate}]`, sanitiza (trim, placa em UPPERCASE, ignora entradas sem nome) e salva via `storage.updateServiceOrder`. SO_LIST_COLS atualizado em `server/routes/service-orders.ts` para retornar `extra_drivers` na listagem.

### Início Antecipado da Missão — Liberado por Ciência (sem gate de horário)
- **Regra de operação:** O agente avança para a próxima etapa assim que confirma "Declaro ciência desta missão", independentemente do horário agendado e sem necessidade de aprovação da central.
- **Frontend (`client/src/pages/mobile/missao.tsx`):** Removidos os blocos "Aguarde o Horário" / "Início Antecipado" da etapa `aguardando` e o estado/effect `earlyBlocked`. O botão "Iniciar Missão" fica habilitado quando `cienteConfirmed === true`.
- **Backend (`server/routes/mission.ts`):** Removidos os bloqueios `EARLY_START_BLOCKED` (POST `/api/mission/advance` e a variante simulada). O cálculo `diffMin > 30 && !earlyStartApproved` foi substituído por comentário explicando a regra. A coluna `early_start_approved` permanece (ainda usada por OperationalGrid e endpoint de autorização) caso a regra mude no futuro.

### Cancelamento de NF (refletindo cancelamentos da prefeitura)
- **Problema:** Quando a NF é cancelada diretamente no portal da prefeitura (fora do Asaas), o sistema continua exibindo `nfse_status=AUTHORIZED` e o valor segue contando como receita. Caso real: NF 242 cancelada na prefeitura aparecia como AUTORIZADO.
- **Endpoint `POST /api/invoices/:id/cancel-nfse`** (`server/asaas.ts`): aceita `{ localOnly: boolean, reason?: string }`. Quando `localOnly=true`, **pula a chamada ao Asaas** e apenas marca `nfse_status='CANCELED'` no banco (uso quando a NF já foi cancelada externamente). Quando `localOnly=false`, mantém o fluxo original (cancela no Asaas + marca local). Restrito a `role=diretoria`. Auditado em `system_audit_log`.
- **UI `client/src/pages/admin/relatorio-nf.tsx`:** botão **"Cancelar"** vermelho em cada linha de fatura com NF emitida (visível apenas para diretoria), abre modal com 2 opções: (1) "Cancelar no Asaas + local" e (2) "Apenas marcar como cancelada (cancelamento já feito na prefeitura)" + campo de motivo (até 500 chars, registrado no log).
- **Receita exclui canceladas — Relatório de NF:** `GET /api/relatorio-nf` agora calcula o card "Total no período" excluindo registros com `normalizedStatus === "NF_CANCELADA"`. O card "Cancelada" continua visível separadamente para auditoria.
- **Receita exclui canceladas — Balanço Gerencial / Dashboard Financeiro:** `GET /api/financial/dashboard` (`server/routes/escort.ts`) carrega `invoices` com `nfse_status` cancelado e marca os `service_order_id` correspondentes (direto da invoice ou via `escort_billings.invoice_id`). A função `calcFat(b)` retorna 0 para billings cujo SO está nesse Set, zerando faturamento, lucro, vendas-por-dia e totais por viatura/agente.

### Relatório de NF — Filtro por CNPJ Emitente (Torres)
- **Constante `TORRES_CNPJ = "36982392000189"`** em `server/asaas.ts` representa o emitente único de NFs do sistema (Torres Vigilância Patrimonial).
- **Coluna `provider_cnpj TEXT`** em `invoices` (`shared/schema.ts` + ALTER idempotente em `server/db-init.ts`, com índice `idx_invoices_provider_cnpj`) marca cada fatura com o CNPJ do emitente no momento da criação.
- **Inserts em `server/asaas.ts`** (POST `/api/invoices` individual e POST `/api/boletim-medicao/gerar-fatura/:clientId` consolidado) gravam `provider_cnpj: TORRES_CNPJ`.
- **GET `/api/relatorio-nf` (`server/asaas.ts`):** após buscar `invoicesRaw`, filtra `cleanCnpj(provider_cnpj) === TORRES_CNPJ`. Registros legados sem `provider_cnpj` (NULL) são presumidos como Torres (mantém compatibilidade). Linhas BOL (boletim_approvals) sempre são da Torres. Loga quantidade ocultada.

### Balanço Gerencial — Correções de Cálculo (`client/src/pages/admin/balanco-gerencial.tsx`)
- **Folha contada em DOBRO eliminada:** transações `origin_type=payroll` já entram em `expenseSums.total`. A `provisaoRH` (estimativa CCT) era somada por cima → custo inflado. Novo cálculo: `folhaConsiderada = max(provisaoRH, desp_folha)` e `custoTotal = pag + (despReais − desp_folha) + folhaConsiderada` (cobre o gap quando a folha real ainda não foi totalmente lançada).
- **Breakdown do card Custos Totais:** adicionada linha "Folha realizada" (de `desp_folha` quando > 0). Provisão RH renomeada para "Provisão RH (CCT) — Estimada", com nota "RH considerado: max(realizada × provisão)".
- **Custo/km usa hodômetro:** combustível é gasto rodando TODOS os quilômetros da viatura, não só o km faturado em missões. Quando `eficiencia.totalKm > 0` (leitura de hodômetro), `custoPorKm = combTotal / eficiencia.totalKm`. Fallback para `kmTotal` (missões) quando não há hodômetro. Indicador `(hodômetro)` / `(missões)` ao lado do valor + tooltip explicativo.
- **Tooltips de fonte de dados:** título do card mudou para "KM Rodado (missões)" com tooltip explicando que o KM total real (hodômetro) está no card Eficiência.

### Custos Fixos / TCO / Margem 35% (`/admin/custos-fixos` + `balanco-gerencial.tsx`)
- **Tabela `fixed_costs`** (id, description, category, monthly_value, due_day, active, notes). Categorias: Aluguel, Utilidades, Serviços, Software, Seguros, Impostos, Outros. Criada idempotente em `db-init.ts`.
- **Colunas adicionais em `employee_salaries`:** `vale_refeicao_mensal`, `vale_transporte_mensal`, `beneficios_outros`, `encargos_pct` (default 80), `horas_mensais` (default 220) — base CCT vigente.
- **Backend** (`server/routes/fixed-costs.ts`): CRUD `/api/fixed-costs`, summary `/api/fixed-costs/summary` (rateio mensal/diário/semanal/anual + por categoria), `/api/fixed-costs/rh-summary` (folha total com encargos + benefícios por agente ativo, custo/hora), e `/api/balanco/tco?from=&to=` (faturamento, custosVariáveis, custosFixosRateados, custosRH, lucro, margem, abaixoMeta). Helpers exportados: `getMonthlyFixedCostsTotal`, `calculateDailyOverhead`, `calculateAgentMonthlyCost`, `calculateAgentCostPerHour`, `getRHCostForPeriod`, `getFixedCostsForPeriod`. Helper `toCamelFixedCost()` normaliza snake_case→camelCase em todos os returns (Supabase REST devolve snake_case mas o tipo `FixedCost` é camelCase).
- **Página `/admin/custos-fixos`:** cards de rateio (mensal/diário/semanal/anual), distribuição por categoria, CRUD com modal (POST/PATCH aceitam `monthlyValue` como string OU número via `z.union → transform String`), seção "Custos de RH (Salários + Benefícios)" com 4 cards (folha mensal, diário, base+encargos, benefícios) + tabela "Custo por Agente" (base, encargos, VR/VT/outros, total mensal, custo/hora), e card final "Custo Total da Operação" (TCO = Custos Fixos + Custos de RH). Link `/admin/employees` para gerir salários.
- **Balanço Gerencial:** `totals.custosFixosRateados` somado aos custos do período, novos cards "Custos Fixos (rateados)", "Lucro Líquido", "Margem Líquida" com meta 35% — badge "ABAIXO DA META" (vermelho) ou "ATENÇÃO" (amarelo) quando margem < 35%.
- **SSOT de status:** `RECUSADA` é o único status canônico (substituiu RECUSADO/CANCELADA em todo o módulo). IA no checklist foi DESCARTADA pelo usuário.


### Ponto Eletrônico — Integração Control iD (iDFace MAX via Cloud)
- **Objetivo:** substituir os 3 fluxos antigos de ponto (Folha Manual, Mobile/Ponto selfie+GPS, Ponto Operacional) por um único fluxo via aparelho biométrico Control iD iDFace MAX.
- **Tabelas (`shared/schema.ts` + migrations em `server/routes.ts`):**
  - `control_id_devices` (cadastro de aparelhos: nome, tipo, base_url, login, password_enc AES-256-GCM, session cache, last_sync_*).
  - `control_id_users_map` (mapping employee_id ↔ control_id_user_id, único por device).
  - `control_id_punches` (batidas brutas com dedup por external_id, employee_id null se não mapeado).
- **Adapter `server/control-id.ts`:** crypto AES-256-GCM (chave derivada de CONTROLID_ENC_KEY||SESSION_SECRET); login tenta /login, /api/login, /api/auth/login (token cacheado 12h); fetchEvents tenta /load_objects (object: access_logs), /api/events, /api/access_logs (suporta variações de tenant); normalizeEvent parse flexível de id/time/direction/source; syncDevice puxa novas batidas desde lastPunch, dedup por external_id, auto-mapeia employee via mappings, atualiza last_sync_*; syncAllDevices itera devices ativos; buildFolhaPonto consolida batidas → 4 marcações por dia BRT.
- **Endpoints `server/routes/control-id.ts` (registrados em `server/routes.ts` linha 931):** CRUD /api/control-id/devices (oculta password_enc); POST /devices/:id/test e /sync; GET /devices/:id/users (cadastro do aparelho); CRUD /api/control-id/mappings (com backfill de batidas órfãs no insert); GET /api/control-id/punches (filtros employeeId/deviceId/from/to/limit); GET /api/control-id/folha/:employeeId?month=YYYY-MM; POST /api/control-id/sync-all (admin).
- **Cron 5min** em `server/cron.ts` chama syncAllDevices automaticamente.
- **Página `/admin/control-id` (`client/src/pages/admin/control-id.tsx`):** 4 tabs — **Aparelhos** (CRUD + Testar + Sync manual), **Mapping Funcionários** (com botão 'Buscar usuários do aparelho' e quick-map de não-mapeados), **Batidas** (filtros funcionário+período, refetch 30s, badge entrada/saída), **Folha de Ponto** (consolidação mensal com entrada/almoço/saída/horas).
- **Sidebar:** item 'Ponto Control iD' adicionado em RH (`client/src/components/admin/layout.tsx`); legacy renomeado para 'Folha de Ponto (legado)'.
- **Pendente (Fase 2):** após cadastrar o aparelho na tela e validar fluxo completo (test/sync/mapping/batidas chegando), DELETAR `/admin/timesheets`, `/mobile/ponto`, `/mobile/ponto-operacional` e `/admin/ponto-operacional` + tabelas órfãs (`ponto_registros`, `timesheets`).

## 🛡️ Anti-Patterns / Memória de Bugs (NÃO REINTRODUZIR)

Esta seção documenta bugs caros já corrigidos. **Antes de mexer nos sistemas
abaixo, leia os pontos correspondentes.** Se você encontrar algum desses
padrões reaparecendo, corrija imediatamente.

### Toasts (`client/src/hooks/use-toast.ts`)
- ❌ **`TOAST_LIMIT = 1`** — descarta avisos legítimos quando vários eventos chegam juntos (chat + alerta + missão). ✅ Manter em 5.
- ❌ **`TOAST_REMOVE_DELAY = 1000000`** (≈17 min) — toast nunca somia da tela. ✅ 5000ms (5s).
- ❌ Disparar toast por evento Realtime sem `dedupKey` — gera 3-4 toasts idênticos para a mesma mensagem (Supabase entrega INSERT em múltiplos canais). ✅ Sempre passar `dedupKey: "chat-${msgId}"` ou similar.
- ❌ ID sequencial `let count = 0` — colide entre instâncias. ✅ `crypto.randomUUID()`.

### Push Notifications (`server/routes/push.ts` + tabela `push_subscriptions`)
- ❌ Armazenar subscriptions em **`Map` na memória do servidor** — somem a cada redeploy/reboot, usuário precisa "se cadastrar" de novo. ✅ Persistir em Supabase (`push_subscriptions` com `endpoint UNIQUE`).
- ❌ `userId` derivado de `Math.random()` ou de algo volátil — quebra associação após relogar. ✅ Usar `req.user.id` (estável) e `req.user.email` como fallback.
- ❌ Endpoint `/api/push/send` público — qualquer um envia push em nome da empresa. ✅ Sempre `requireAuth` (e idealmente `requireAdminRole`).
- ❌ Não desinscrever no logout — usuário continua recebendo push após sair. ✅ Frontend chama `/api/push/unsubscribe` no logout.

### WhatsApp / Z-API
- ❌ Chamar **Z-API direto do navegador** com `CLIENT_TOKEN` no bundle — qualquer cliente com DevTools rouba o token e envia mensagens em nome da empresa. ✅ Sempre via proxy backend autenticado (`/api/whatsapp/*` com `requireAuth`). **Hoje o sistema usa apenas links `wa.me/*` (deeplinks, sem token), o que é seguro. NÃO migrar para chamadas Z-API no frontend.**
- ❌ Canal Realtime hardcoded (ex: `"whatsapp-rt"`) compartilhado entre tabs — mensagens são perdidas/duplicadas. ✅ Canal único por usuário: `whatsapp-rt-${userId}-${Date.now()}`.
- ❌ `alert("...")` para feedback — bloqueia a UI. ✅ `toast({...})`.

### E-mails (`server/routes/_helpers.ts`, `server/asaas.ts`, `server/generate-boletim-omega.ts`, `server/routes/mission.ts`, `server/routes/service-orders.ts`)
- ❌ **`bcc: "a@x, b@x"`** (string) — Office365/Outlook ignora silenciosamente, e-mails de cobrança/boletim "somem". ✅ SEMPRE array: `bcc: ["a@x", "b@x"]`.
- ❌ Criar `nodemailer.createTransport()` a cada e-mail — leak de socket + handshake repetido. ✅ Singleton em `_helpers.ts:createSmtpTransporter()` com `pool: true`.
- ❌ Não validar `EMAIL_PASS`/`SMTP_PASS` antes de enviar — erro confuso e tentativa repetida. ✅ Helper retorna `null` e loga uma vez se faltar credencial.
- ❌ BCC para destinatário externo do cliente — vaza endereços internos da Torres. ✅ BCC vai SOMENTE para `thiago@grupotmseg.com.br` e contatos internos da empresa.

### Cache PWA (`server/static.ts`, `client/public/sw.js`, `client/src/main.tsx`)
- ❌ `express.static(distPath)` sem `setHeaders` — `index.html` fica cacheado pelo navegador apontando para hashes JS antigos após deploy. ✅ `setHeaders` com `no-cache` para `index.html`/`sw.js`/`manifest.json` e `immutable max-age=31536000` para `/assets/*` (já têm hash do Vite).
- ❌ Service Worker sem versionamento — não tem como saber se está velho. ✅ `SW_VERSION` no topo do `sw.js` e limpeza de caches antigos no `activate`.
- ❌ Confiar apenas em `skipWaiting()` para atualizar — em PWA instalada no iOS o SW pode não atualizar nunca. ✅ Cliente chama `GET /api/version` no boot e a cada `visibilitychange`; se versão local ≠ servidor, faz hard reset (SWs + caches + IndexedDB) preservando login. Bumpar `APP_VERSION` em `server/constants.ts` a cada deploy importante.
- ❌ Hard reset apagando `localStorage` inteiro — usuário desloga em todo update. ✅ Preservar `auth_token`, `supabase.auth.token`, `notification_sound_enabled`.
- ❌ Hard reset sem flag de sessão — loop infinito. ✅ `sessionStorage.setItem("__did_hard_reset", "1")` evita re-execução na mesma sessão.
